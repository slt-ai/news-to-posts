# Папка виртуального окружения — не попадает в Git
.venv/

# Кэш Python
__pycache__/
*.pyc

# Настройки запуска (локальные: порт, debug) — не попадают в Git
# При необходимости скопируй .env.example
.env

# Служебные папки редакторов и ОС
.vscode/
.idea/
Thumbs.db
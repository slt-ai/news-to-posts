# Генератор постов по новостям ✍️

Локальный веб-сервис: вставляешь ссылку на новость или статью про ИИ и компьютерные технологии — приложение скачивает страницу, достаёт текст, отправляет его облачной языковой модели **Ollama Cloud** вместе с настройками «голоса» и настроением. Модель пишет оригинальный пост для соцсетей, а ты публикуешь его в сообщество **ВКонтакте** одной кнопкой — сразу или в запланированное время.

---

## Возможности

- 🧠 Генерация поста по любой статье: ссылка → текст страницы → готовый пост.
- 🎭 **Настроение поста** — шесть «чипов»: радостное, восторженное, вдохновляющее, игривое, интригующее, деловое.
- 🗣 **Голос поста** — тон, длина, количество эмодзи и стиль заголовка настраиваются в файле `voice.md`.
- 🤖 **Выбор модели** — список облачных моделей Ollama с ценами прямо на странице.
- 📋 Кнопка «Скопировать пост» с запасным вариантом копирования для старых браузеров.
- ✏️ **Редактирование поста** — кнопка-карандаш: поправь текст прямо на странице, нажми «Сохранить», затем публикуй, откладывай или сохраняй в избранное уже отредактированный вариант.
- 🚀 **Публикация в сообщество ВКонтакте** — кнопка «Опубликовать пост» выкладывает текст на стену сообщества от его имени.
- ⏰ **Отложенные посты** — выбери дату и время в календаре (минимум на 2 минуты позже текущего), ВК сам выложит пост. Очередь отложки видна на странице, любой пост можно отменить. Сервер держать включённым не нужно.
- ⭐ **Избранное** — сохраняй удачные посты локально (файл `data/favorites.json`), копируй, публикуй в ВК или удаляй.
- 🛟 Fallback: если сайт блокирует автоматическое чтение (403/401/498 — защита от ботов), можно описать суть новости своими словами, и пост соберётся по описанию.

## Технологии

- Python 3 · Flask · requests · BeautifulSoup · python-dotenv
- Облачное API: [Ollama Cloud](https://ollama.com) (`/api/chat`)
- Публикации: [VK API](https://dev.vk.com/ru/method/wall.post) (`wall.post`, `wall.get`, `wall.delete`)

## Структура проекта

```
site-generation-post/
├── app.py                 # вся логика сервера (Flask + API модели + VK + избранное)
├── templates/index.html   # страница с формой, постом, отложкой и избранным
├── static/style.css       # стили
├── static/script.js       # копирование, редактирование, состояние «Готовим…», минимум даты отложки
├── screenshots/           # скриншоты-примеры генерации
├── voice.md               # настройки «голоса» поста
├── ВК_ИНСТРУКЦИЯ.md       # подключение к сообществу ВК простым языком
├── data/favorites.json    # избранное (создаётся само, НЕ в Git)
├── .env                   # локальные настройки (ключи API!) — НЕ в Git
├── .env.example           # шаблон настроек для копирования
├── requirements.txt       # зависимости Python
├── start.bat              # запуск в один клик (Windows)
└── README.md
```

---

## Примеры генерации

- [Пример 1: генерация поста, публикация в ВК и отложка](screenshots/screenshot-1.png)
- [Пример 2: очередь отложенных постов и избранное](screenshots/screenshot-2.png)

---

## Установка и запуск

### 1. Требования

- **Python 3.10+** ([python.org](https://www.python.org/downloads/)) — при установке на Windows отметь галочку **«Add python.exe to PATH»**.
- **Git** (в любой ОС) или просто скачать архив репозитория.

### 2. Склонируй проект

```bash
git clone https://github.com/slt-ai/news-to-posts.git
cd news-to-posts
```

### 3. Получи ключ API Ollama

1. Зарегистрируйся на [ollama.com](https://ollama.com) — у аккаунтов есть бесплатные кредиты.
2. Создай API-ключ: [ollama.com/settings/keys](https://ollama.com/settings/keys) (именно API-ключ, не SSH).
3. Скопируй его — он понадобится на шаге 5.

### 4. Создай виртуальное окружение и установи зависимости

**Windows (PowerShell или командная строка):**

```powershell
python -m venv .venv
.venv\Scripts\activate
python -m pip install -r requirements.txt
```

**macOS / Linux:**

```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
```

### 5. Настрой `.env`

Скопируй шаблон и открой его в редакторе:

**Windows:**

```powershell
copy .env.example .env
notepad .env
```

**macOS / Linux:**

```bash
cp .env.example .env
```

Впиши свой ключ вместо пустого значения:

```
OLLAMA_API_KEY=твой_ключ_здесь
```

Остальные настройки (хост, порт, `DEBUG`, модель по умолчанию) можно оставить как есть. Файл `.env` уже добавлен в `.gitignore` — он не попадёт в Git.

> Хочешь публиковать посты в сообщество ВКонтакте — заполни в том же `.env` поля `VK_ACCESS_TOKEN` и `VK_GROUP_ID` (заготовки уже есть в конце файла). Как их получить — см. раздел «Подключение ВКонтакте» ниже.

### 6. Запусти

**Windows — в один клик:** просто запусти `start.bat`.

**Или вручную (любая ОС):**

```bash
python app.py
```

Теперь открой в браузере: **http://127.0.0.1:5000**

> Не закрывай окно, где запущен сервер, пока пользуешься сайтом.

---

## Как пользоваться

1. Вставь ссылку на новость или статью про ИИ / технологии в первое поле.
2. *(Необязательно)* Допиши своими словами важные акценты или своё мнение — модель учтёт это.
3. Выбери **настроение поста** (чип-эмодзи).
4. Выбери **модель** — на странице показано имя и цена за миллион токенов.
5. Нажми **«Сформировать пост»**.
6. Когда пост готов:
   - **«Скопировать пост»** — скопировать текст в буфер обмена;
   - **«✏️ Редактировать»** — поправить текст прямо на странице (кнопка станет «💾 Сохранить», рядом появится «Отмена»); сохранённые правки подхватят все кнопки ниже;
   - **«🚀 Опубликовать пост»** — сразу выложить на стену сообщества ВК;
   - **«⭐ В избранное»** — сохранить пост локально, чтобы не потерять;
   - **«⏰ Отложенный пост»** — выбери дату и время, нажми «Запланировать», и ВК выложит пост сам.
7. В разделах **«⏰ Отложенные посты»** и **«⭐ Избранное»** внизу страницы — очередь публикаций (можно отменить) и сохранённые посты (можно копировать, публиковать, удалять).

### Если сайт «не открывается»

Многие новостные и магазинные сайты (Wildberries, DNS-шоп, часть СМИ) блокируют скриптовые GET-запросы и возвращают коды **401 / 403 / 498** — это защита от ботов, а не баг. Решение:

- опиши суть новости **своими словами** в поле «Дополнительно о новости»;
- нажми «Сформировать пост» — пост соберётся по твоему описанию (страница появится уведомление об этом).

## Подключение ВКонтакте

Нужны 2 значения в `.env` (подробная инструкция простым языком — в файле `ВК_ИНСТРУКЦИЯ.md`):

| Параметр | Что это | Где взять |
|---|---|---|
| `VK_GROUP_ID` | Номер сообщества (только цифры) | Цифры в ссылке `vk.com/club123456789` или Управление → Настройки → ID сообщества |
| `VK_ACCESS_TOKEN` | Секретный ключ сообщества | Сообщество → Управление → Работа с API → Создать токен (права «Стена» и «Фотографии») |

Впиши их в `.env`, перезапусти сервер — на странице появится «✅ ВКонтакте подключён», кнопки публикации станут активны. Рекомендуемая тематика сообщества в ВК: **«Компьютер и интернет»**.

Правила отложки: время минимум на 2 минуты позже текущего и не дольше чем на год вперёд.

## Настройка «голоса» поста (`voice.md`)

Файл читается при каждом поколении. Формат — `КЛЮЧ: значение`, строки с `#` — комментарии:

| Ключ | Допустимые значения |
|---|---|
| `ТОН ПОСТА` | дружелюбный, официальный, весёлый, вдохновляющий, дерзкий, … |
| `ДЛИНА ПОСТА` | короткий, средний, длинный |
| `КОЛИЧЕСТВО ЭМОДЗИ` | число от 0 до 5 |
| `СТИЛЬ ЗАГОЛОВКА` | заглавные буквы, обычный |

## Модели и цены

По умолчанию стоит **Gemma 4** — пишет пост сразу. Актуальные цены и список моделей: [ollama.com/pricing](https://ollama.com/pricing).

Модели с режимом «мышления» (например, GPT-OSS, GLM) могут тратить лимит токенов на внутренние рассуждения и выдавать пустой или «ломаный» ответ — поэтому запрос всегда отправляется с параметром `think: false`.

## Устранение проблем

| Проблема | Решение |
|---|---|
| `OLLAMA_API_KEY` не заполнен | Впиши ключ в `.env` (без пробелов и кавычек): [ollama.com/settings/keys](https://ollama.com/settings/keys) |
| `401` при запросе к модели | Неверный/неактивный ключ — проверь, что скопирован целиком |
| `403` | Модель недоступна на твоём тарифе — выбери другую или пополни баланс |
| `429` | Закончились кредиты или слишком много запросов — подожди |
| Сайт блокирует чтение страницы | Опиши новость своими словами (см. выше) |
| «ВКонтакте не подключён» | Заполни `VK_ACCESS_TOKEN` и `VK_GROUP_ID` в `.env` и перезапусти сервер |
| Ошибка ВК 5 | Токен неправильный или просрочен — создай новый (Работа с API → Создать токен) |
| Ошибка ВК 7 / 15 | Нет прав — выдай токену право на стену, проверь, что ты админ сообщества |
| Ошибка ВК 214 | Стена закрыта — Управление → Разделы → Стена: «Открытая» или «Ограниченная» |
| «Выбери время хотя бы на 2 минуты позже» | Отложка слишком близко к текущему времени — выбери время подальше |